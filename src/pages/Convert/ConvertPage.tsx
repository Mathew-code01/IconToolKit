// src/pages/Convert/ConvertPage.tsx

export default function ConvertPage() {
  return (
    <section className="mx-auto w-full max-w-7xl px-4 py-12 sm:px-6 lg:px-8">
      <p className="text-sm font-semibold text-[#6366F1]">Convert</p>

      <h1 className="mt-2 text-3xl font-bold tracking-tight text-[var(--text)]">
        Convert your files
      </h1>

      <p className="mt-4 text-[var(--text-secondary)]">
        Convert images between PNG, JPG, WebP, SVG, ICO, PDF, and other
        supported formats.
      </p>
    </section>
  );
}